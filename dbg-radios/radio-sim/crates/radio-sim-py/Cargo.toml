[package]
name = "radio-sim-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "radio_sim"
crate-type = ["cdylib"]

[dependencies]
radio-sim-core = { path = "../radio-sim-core" }
pyo3 = { version = "0.24", features = ["extension-module"] }
rayon = "1.10"
toml = "0.8"
